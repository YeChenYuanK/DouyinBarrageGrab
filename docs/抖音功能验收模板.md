# 抖音功能验收模板（用于对标快手）

## 基本信息

- 验收日期：
- 验收人：
- 分支/提交：
- 直播间：
- 客户端进程：

## A. 连接与订阅验收

| 编号 | 验收项 | 通过标准 | 证据（日志/截图） | 结果 |
|---|---|---|---|---|
| A1 | 命中抖音 WS 规则 | 出现订阅日志，且 URI 命中 `webcast\d+-ws-web-\w+\.(douyin\|amemv)\.com` |  | 通过 / 不通过 |
| A2 | DataReceived 已挂载 | 命中后有持续 WS 收包日志 |  | 通过 / 不通过 |
| A3 | 非目标连接不误判 | 非抖音 WS 不进入抖音解析分支 |  | 通过 / 不通过 |

## B. 协议解析验收

| 编号 | 验收项 | 通过标准 | 证据（日志/样本） | 结果 |
|---|---|---|---|---|
| B1 | WssResponse 外层解析 | 能反序列化且命中 `compress_type=gzip` |  | 通过 / 不通过 |
| B2 | gzip 解压 + Response 内层解析 | 可解析出 `response.Messages`，消息数 > 0 |  | 通过 / 不通过 |
| B3 | HTTP protobuf 解析 | 轮询回包可解析并进入统一分发 |  | 通过 / 不通过 |
| B4 | 异常不阻断主流程 | 单包失败后后续包仍可继续处理 |  | 通过 / 不通过 |

## C. 消息类型验收

| 编号 | Method | 通过标准 | 样本数量 | 证据 | 结果 |
|---|---|---|---|---|---|
| C1 | WebcastChatMessage | 能触发 `OnChatMessage` |  |  | 通过 / 不通过 |
| C2 | WebcastLikeMessage | 能触发 `OnLikeMessage` |  |  | 通过 / 不通过 |
| C3 | WebcastGiftMessage | 能触发 `OnGiftMessage` |  |  | 通过 / 不通过 |
| C4 | WebcastMemberMessage | 能触发 `OnMemberMessage` |  |  | 通过 / 不通过 |
| C5 | WebcastSocialMessage | 能触发 `OnSocialMessage` |  |  | 通过 / 不通过 |
| C6 | WebcastRoomUserSeqMessage | 能触发 `OnRoomUserSeqMessage` |  |  | 通过 / 不通过 |
| C7 | WebcastControlMessage | 能触发 `OnControlMessage` |  |  | 通过 / 不通过 |
| C8 | WebcastFansclubMessage | 能触发 `OnFansclubMessage` |  |  | 通过 / 不通过 |

## D. 去重与稳定性验收

| 编号 | 验收项 | 通过标准 | 证据 | 结果 |
|---|---|---|---|---|
| D1 | 消息去重有效 | 重复 msgId 不重复触发业务事件 |  | 通过 / 不通过 |
| D2 | 长时运行稳定 | 连续运行 ___ 分钟，解析不中断 |  | 通过 / 不通过 |
| D3 | 轮询降级有效 | WS 断流后轮询仍能持续出消息 |  | 通过 / 不通过 |

## E. 配置与输出验收

| 编号 | 验收项 | 通过标准 | 证据 | 结果 |
|---|---|---|---|---|
| E1 | printFilter 生效 | 仅打印选定类型 |  | 通过 / 不通过 |
| E2 | pushFilter 生效 | 仅推送选定类型 |  | 通过 / 不通过 |
| E3 | logFilter 生效 | 仅记录选定类型 |  | 通过 / 不通过 |
| E4 | polling 配置生效 | `enabled/interval` 按配置执行 |  | 通过 / 不通过 |

## F. 回归结论

- 抖音链路是否回归通过：通过 / 不通过
- 快手对标差距项：
  1.
  2.
  3.
- 下一步修复动作：
  1.
  2.
  3.

## 附：建议日志关键字

- 抖音订阅：`[抖音] 订阅到新的弹幕流地址`
- 抖音解析：`[WS] 抖音解析成功`
- 轮询链路：`BARRAGE_POOL_PATH` 命中相关日志
- 异常信息：`处理弹幕数据包时出错`
